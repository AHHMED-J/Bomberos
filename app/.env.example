# Copia este archivo a .env y ajusta los valores. El .env NO se sube al repo.

# --- MySQL local ---------------------------------------------------------
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=parte_digital

# --- Servidor ------------------------------------------------------------
PORT=3000
